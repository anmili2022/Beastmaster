# 驯兽 ACR 设计说明

## 目标

驯兽 ACR（`驯兽ACR`）负责在玩家明确开启自动输出后，围绕当前手动选择的目标执行基础连击和捕获流程，并为高级技能提供可控的资源判断与开关。

设计原则：

- 默认不自动输出。
- 默认不自动使用高级技能。
- 只对玩家当前手动选择的目标执行动作。
- 捕获状态必须校验 `StatusId` 和 `SourceId`。
- 量谱和运行时状态读取只读，不写入游戏内存。
- 任何关键状态无法确认时，停止高级技能动作并显示原因。

## 当前已实现

### 基础自动输出

基础技能顺序：

```text
碎击斩 → 碎咬斧 → 裂盾劈
```

执行前检查：

- 自动输出已开启。
- 玩家为驯兽师。
- 玩家存在、存活且没有读条。
- 不在切图、骑乘或剧情占用状态。
- 当前目标是可选中的 BattleNpc。
- 目标存在、存活且 HP 大于 0。
- `ActionManager` 可用且允许使用目标技能。

### 捕获流程

捕获开关开启时：

```text
目标血量 < 捕获阈值
且目标没有自身施加的捕获状态
→ 选择捕获
```

目标血量高于阈值时继续基础连击。捕获阈值范围为 `1%~100%`，默认 `80%`，可在主页面和悬浮窗修改。

捕获状态来源：

```text
status.StatusId == 捕获状态
且 status.SourceId == LocalPlayer.EntityId
→ 自身捕获状态
```

他人施加的同名状态不会阻止自身捕获判断。

捕获请求发送后进入短暂等待窗口，等待自身 `SourceId` 状态刷新；等待期间不会重复请求捕获，也不会继续对目标造成伤害。目标切换、目标失效或自动输出关闭时重置捕获状态。

### 量谱读取

从以下地址只读采样 48 字节：

```text
JobGaugeManager.Instance()->CurrentGauge
```

字段：

| 字段 | 偏移 | 上限/拆分 |
| --- | --- | --- |
| 技力 | `+0x10` | `250` |
| 兽力 | `+0x11` | `250` |
| 当前兽笛 | `+0x13` | `1~3` 为对应兽笛 |
| 御兽之心 | `+0x18` | `raw >> 2` |
| 兽灵之心 | `+0x18` | `raw & 3` |

采样周期为 100ms，UI 使用缓存快照。

## 魔兽资料模型

图鉴编号与召唤物 DataId 的关系：

```text
DataId = 18915 + 图鉴编号
```

每个图鉴条目提供统一技能资料：

```text
BeastmasterSkillProfile
├── 图鉴编号
├── 魔兽名称
├── SummonDataId
├── 属性：猛 / 坚 / 魔 / 翔
├── 大招 ActionId
└── 释放 ActionId
```

注意：图鉴中的“大招 ActionId”是当前魔兽对应的宠物技能资料，用于识别属性；玩家实际释放驯兽师职业大招使用固定的 ActionId `47093`。

属性由大招 IconId 映射：

```text
3906 → 猛
3907 → 坚
3908 → 魔
3909 → 翔
```

## 高级技能开关

悬浮窗和主窗口都提供：

```text
高级技能
御兽协作（黄豆）
兽灵协作（蓝豆）
释放
```

开关关系：

```text
高级技能关闭
→ 所有高级技能保留资源

高级技能开启 + 两个协作模式均关闭
→ 不使用协作技和大招

御兽协作（黄豆）和兽灵协作（蓝豆）互斥
→ 同时开启时自动关闭另一项

释放关闭时，不自动请求当前召唤兽的 `ReleaseActionId`；手动协作流程仍由对应的黄豆/蓝豆选项控制。
```

高级技能支持两种调用路径：

- 手动大招按钮：通过悬浮窗主动请求固定职业大招 `47093`。
- 自动协作：在高级技能总开关、对应协作模式、当前魔兽和资源条件均满足时，按模式接入自动技能选择；大招由协作流程自动包含。
- 魔兽释放技能：高级技能总开关开启后，以基础 ActionId `44890` 调用 `GetAdjustedActionId` 获取当前召唤兽的实际释放 ID，再使用 `ActionType.Action` 对当前目标请求；只有 `GetActionStatus == 0` 才调用 `UseAction`，冷却或其他状态码非零时回退基础连击，不阻塞 1-2-3。

大招调用会保留目标、职业、量谱和 `ActionManager` 检查。`GetActionStatus` 返回非零时仍尝试一次 `UseAction`，由动作系统决定是否接受；基础连击和捕获不使用该兜底。

协作技按两段候选流程请求：第一段成功后保存按当前魔兽属性选择的第二段，经过动作保护时间后直接请求第二段；属性状态只用于诊断，不再作为第二段的硬阻塞条件。运行时协作窗口字段仍需上线后确认。

## 技能决策优先级

计划中的决策顺序：

```text
角色状态
→ 当前目标
→ 捕获判断
→ 基础连击
→ 魔兽/属性识别
→ 技力和兽力判断
→ 高级技能开关
→ ActionManager 可用性
→ 高级技能释放
```

当高级技能未开启或关键数据缺失时，回退到基础连击，不影响捕获安全判断。

## 当前提示状态

悬浮窗可以显示：

```text
当前魔兽和属性
目标名称和血量
捕获阈值
自身/他人捕获状态
技力 / 兽力
御兽之心 / 兽灵之心
当前技能和失败原因
高级技能候选
```

## 待实现

- 大招手动释放按钮。
- 只提示/手动释放/自动释放三种模式。
- 协作流程中大招实际释放的运行时验证和节流优化。
- 协作技运行时窗口读取。
- 属性循环和生息/死灭状态识别。
- 自动协作技的运行时窗口验证和释放优化。
- 连续技能失败后的自动暂停保护。
- 切换目标、切图、召唤兽死亡后的状态机重置。

## 上线验证清单

验证时优先使用 DEBUG 页的“读取协力验证数据”按钮。该按钮只读输出量谱、当前魔兽、目标、关键 Action 状态码和自身属性状态。

### 验证步骤

1. 进入安全区域，切换驯兽师，不开自动输出，召唤任意魔兽。
2. 点击“读取协力验证数据”，记录初始技力、兽力、心数、兽笛、魔兽属性和自身属性状态。
3. 只使用普通技能，等待量谱变化，再点击一次按钮，确认 `+0x10` 和 `+0x11` 的变化方向。
4. 开启自动输出和高级技能，但先关闭两个协力模式，确认基础连击不受影响。
5. 开启“御兽协作（黄豆）”，在技力、兽力达到要求后记录悬浮窗下一技能；确认是否先请求 `47093`。
6. 第一段技能完成后立即点击“读取协力验证数据”，记录自身属性状态、Action 状态码和量谱变化。
7. 在 7 秒内再次点击按钮，确认第二段属性斧所需状态和实际 Action 状态码。
8. 关闭黄豆，开启“兽灵协作（蓝豆）”，重复步骤 5~7，确认第一段是否为属性斧、第二段是否为 `47093`。
9. 分别测试两种模式同时开启，确认 UI 自动关闭其中一个，不能同时运行。
10. 测试切换目标、目标死亡、召唤兽消失和切换职业，确认待续段会被清除且不会对旧目标继续请求技能。

每个阶段至少回传：

- 悬浮窗截图或文字：下一技能、原因、捕获状态。
- “读取协力验证数据”的完整输出。
- 实际是否释放了第一段和第二段。
- 两段技能之间的时间间隔。

- 对照原生量谱确认技力和兽力变化。
- 确认大招实际资源消耗和 ActionManager 状态。
- 确认当前兽笛字段在召唤、切换和消失时的值。
- 采集协作量谱状态和 7 秒协作窗口。
- 确认生息/死灭中心状态字段。
- 验证自身捕获与他人捕获状态的 SourceId。
- 验证目标切换、捕获失败、目标死亡和召唤兽消失。

## 安全边界

以下任一条件不满足时，不得自动释放高级技能：

- 量谱读取失败。
- 当前魔兽未知。
- 属性未知。
- ActionId 无效。
- 目标无效。
- 资源门槛未知。
- 协作窗口未知。
- ActionManager 拒绝使用。
